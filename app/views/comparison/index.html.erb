<div class="max-w-5xl mx-auto">

  <!-- 比較入力エリア -->
  <div class="grid md:grid-cols-2 gap-8 relative">

    <!-- 商品A -->
    <div class="bg-white rounded-2xl shadow p-6">
      <h2 class="text-xl font-bold text-center mb-6">商品A</h2>

      <div class="space-y-4">
        <div>
          <label class="text-sm text-gray-500">内容量</label>
          <input type="number"
                  class="w-full mt-1 p-3 rounded-lg bg-gray-100 border-none focus:ring-2 focus:ring-green"
                  placeholder="例：500">
        </div>

        <div>
          <label class="text-sm text-gray-500">パック数</label>
          <input type="number"
                  class="w-full mt-1 p-3 rounded-lg bg-gray-100 border-none focus:ring-2 focus:ring-green"
                  placeholder="例：3">
        </div>

        <div>
          <label class="text-sm text-gray-500">価格</label>
          <input type="number"
                  class="w-full mt-1 p-3 rounded-lg bg-gray-100 border-none focus:ring-2 focus:ring-green"
                  placeholder="例：598">
        </div>
      </div>
    </div>

    <!-- VSバッジ -->
    <div class="hidden md:flex absolute inset-0 items-center justify-center pointer-events-none">
      <div class="bg-green text-white w-12 h-12 rounded-full flex items-center justify-center font-bold shadow">
        VS
      </div>
    </div>

    <!-- 商品B -->
    <div class="bg-white rounded-2xl shadow p-6">
      <h2 class="text-xl font-bold text-center mb-6">商品B</h2>

      <div class="space-y-4">
        <div>
          <label class="text-sm text-gray-500">内容量</label>
          <input type="number"
                  class="w-full mt-1 p-3 rounded-lg bg-gray-100 border-none focus:ring-2 focus:ring-green"
                  placeholder="例：500">
        </div>

        <div>
          <label class="text-sm text-gray-500">パック数</label>
          <input type="number"
                  class="w-full mt-1 p-3 rounded-lg bg-gray-100 border-none focus:ring-2 focus:ring-green"
                  placeholder="例：3">
        </div>

        <div>
          <label class="text-sm text-gray-500">価格</label>
          <input type="number"
                  class="w-full mt-1 p-3 rounded-lg bg-gray-100 border-none focus:ring-2 focus:ring-green"
                  placeholder="例：498">
        </div>
      </div>
    </div>
  </div>

  <!-- 計算結果 -->
  <div class="mt-12">
    <h3 class="text-lg font-bold mb-4">計算結果（単価比較）</h3>

    <div class="grid md:grid-cols-2 gap-6">

      <!-- 商品A結果 -->
      <div class="bg-white rounded-2xl shadow p-6 text-center">
        <p class="text-gray-500 text-sm">商品A 単価</p>
        <p class="text-3xl font-bold mt-2">
          円
        </p>
        <button class="mt-6 bg-green hover:bg-green text-white px-6 py-2 rounded-full">
          登録する ＋
        </button>
      </div>

      <!-- 商品B結果 -->
      <div class="bg-white rounded-2xl shadow p-6 text-center">
        <p class="text-gray-500 text-sm">商品B 単価</p>
        <p class="text-3xl font-bold mt-2">
          円
        </p>
        <button class="mt-6 bg-green hover:bg-green text-white px-6 py-2 rounded-full">
          登録する ＋
        </button>
      </div>
      </div>
    </div>
  </div>

  <!-- お得表示 -->
  <div class="mt-8">
    <div class="bg-orange-100 text-orange-600 text-center py-4 rounded-xl font-semibold text-lg">
      の方が
      <span class="text-2xl font-bold">
        円
      </span>
      お得です
    </div>
  </div>

</div>